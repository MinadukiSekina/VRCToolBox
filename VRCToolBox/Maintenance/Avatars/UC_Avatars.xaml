<UserControl x:Class="VRCToolBox.Maintenance.Avatars.UC_Avatars"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:VRCToolBox.Maintenance.Avatars"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=local:VM_Avatars}"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <ui:CommandBar DockPanel.Dock="Top" VerticalContentAlignment="Center">
            <ui:CommandBar.Content>
                <ui:AutoSuggestBox ui:ControlHelper.PlaceholderText="アバターの名前を入力して検索……" MinWidth="300" VerticalContentAlignment="Center" QueryIcon="Find"
                                   Margin="5,0,0,0" Text="{Binding SearchText.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                </ui:AutoSuggestBox>
            </ui:CommandBar.Content>
            <ui:AppBarButton Icon="Add" Label="新規追加" Visibility="Visible"/>
        </ui:CommandBar>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*"  />
            </Grid.ColumnDefinitions>
            <Border x:Name="C_Dummy0" Grid.Column="0" HorizontalAlignment="Stretch" Visibility="Hidden"/>
            <Border x:Name="C_Dummy1" Grid.Column="1" HorizontalAlignment="Stretch" Visibility="Hidden"/>
            <Border x:Name="C_Dummy2" Grid.Column="2" HorizontalAlignment="Stretch" Visibility="Hidden"/>
        </Grid>
        <ListView ItemsSource="{Binding Avatars}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center">
            <ListView.Resources>
                <DataTemplate x:Key="AvatarName">
                    <TextBlock Text="{Binding AvatarName.Value}" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                </DataTemplate>
                <DataTemplate x:Key="AuthorName">
                    <TextBlock Text="{Binding AuthorName.Value}" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                </DataTemplate>
                <DataTemplate x:Key="Buttons">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Button>
                            <Viewbox>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Edit"></ui:SymbolIcon>
                                    <TextBlock Text="修正"/>
                                </StackPanel>
                            </Viewbox>
                        </Button>
                        <Button>
                            <Viewbox>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Cancel"></ui:SymbolIcon>
                                    <TextBlock Text="キャンセル"/>
                                </StackPanel>
                            </Viewbox>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn Width="{Binding ActualWidth, ElementName=C_Dummy0}" CellTemplate="{StaticResource ResourceKey=AvatarName}"/>
                    <GridViewColumn Width="{Binding ActualWidth, ElementName=C_Dummy1}" CellTemplate="{StaticResource ResourceKey=AuthorName}"/>
                    <GridViewColumn Width="{Binding ActualWidth, ElementName=C_Dummy2}" CellTemplate="{StaticResource ResourceKey=Buttons}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
