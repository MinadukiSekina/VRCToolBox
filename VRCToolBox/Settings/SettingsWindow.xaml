<Window x:Class="VRCToolBox.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRCToolBox.Settings"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <local:ProgramSettings/>
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <TabControl DockPanel.Dock="Top" TabStripPlacement="Left">
                <TabControl.Resources>
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="FontFamily" Value="Meiryo"/>
                        <Setter Property="FontSize" Value="16"/>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="VRChat ログ">
                    <Grid Background="#FFE5E5E5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="8*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="VRChatがログを書き出す場所" Grid.Column="1" Margin="0,10,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,40,0,0" TextWrapping="Wrap" Text="{Binding VRChatLogPath}" VerticalAlignment="Top"/>
                        <Label Content="VRChatのログをコピーする場所" Grid.Column="1" Margin="0,80,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,110,0,0" TextWrapping="Wrap" Text="{Binding MovedPath}" VerticalAlignment="Top"/>
                    </Grid>
                </TabItem>
                <TabItem Header="写真">
                    <Grid Background="#FFE5E5E5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="8*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="VRChatが写真を保存する場所" Grid.Column="1" Margin="0,10,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,40,0,0" TextWrapping="Wrap" Text="{Binding PicturesSavedFolder}" VerticalAlignment="Top"/>
                        <Label Content="写真を整理して保存する場所" Grid.Column="1" Margin="0,80,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,110,0,0" TextWrapping="Wrap" Text="{Binding PicturesMovedFolder}" VerticalAlignment="Top"/>
                        <Label Content="投稿予定の写真を保存する場所" Grid.Column="1" Margin="0,150,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,180,0,0" TextWrapping="Wrap" Text="{Binding PicturesSelectedFolder}" VerticalAlignment="Top"/>
                        <Label Content="実際に投稿した写真を保存する場所" Grid.Column="1" Margin="0,220,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,250,0,0" TextWrapping="Wrap" Text="{Binding PicturesUpLoadedFolder}" VerticalAlignment="Top"/>
                        <CheckBox Content="月毎・日付毎にフォルダを作成する" IsChecked="{Binding MakeDayFolder, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Column="1" Margin="0,290,0,0" />
                    </Grid>
                </TabItem>
                <TabItem Header="Unity">
                    <Grid Background="#FFE5E5E5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="8*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Unityのプロジェクトを作成しているフォルダ" Grid.Column="1" Margin="0,10,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,40,0,0" TextWrapping="Wrap" Text="{Binding UnityProjectDirectory}" VerticalAlignment="Top"/>
                        <!--<CheckBox Content="月毎・日付毎にフォルダを作成する" IsChecked="{Binding MakeDayFolder, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Column="1" Margin="0,290,0,0" />-->
                    </Grid>
                </TabItem>
                <TabItem Header="データ">
                    <Grid Background="#FFE5E5E5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="8*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="データを保存する場所" Grid.Column="1" Margin="0,10,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,40,0,0" TextWrapping="Wrap" Text="{Binding DBDirectoryPath}" VerticalAlignment="Top"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>
